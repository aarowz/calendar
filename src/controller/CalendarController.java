// Dreshta Boghra & Aaron Zhou
// CS3500 HW4

package controller;

/**
 * Manages the flow of user interaction — reading raw commands, parsing them into executable
 * objects, executing those commands on the model, and displaying results/errors via the view.
 */
public class CalendarController {
}
