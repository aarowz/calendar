// Dreshta Boghra & Aaron Zhou
// CS3500 HW4

package controller;

/**
 * Represents a command to query events or status from the calendar model.
 * Supports queries for events on a specific day, within a date-time range,
 * or checking availability (busy/available) at a given time.
 * Outputs results via the view.
 */
public class QueryEventsCommand implements ICommand {
}
