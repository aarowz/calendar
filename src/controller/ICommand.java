// Dreshta Boghra & Aaron Zhou
// CS3500 HW4

package controller;

/**
 * Represents a generic command that can be executed on the calendar model and view.
 * All command implementations (create, edit, query, exit) must implement this interface.
 * Supports the Command design pattern for uniform command handling.
 */
public interface ICommand {
}
